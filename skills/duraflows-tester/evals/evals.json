{
  "skill_name": "duraflows-tester",
  "evals": [
    {
      "id": 1,
      "prompt": "I have an order processing duraflows workflow. I need to write a vitest test that creates the workflow, triggers a 'start_processing' event, and verifies it transitions from 'pending' to 'processing' with the right context values set by the command. The command calls an external inventory API. Show me the full test setup.",
      "expected_output": "Should set up InMemoryInstanceStore, InMemoryHistoryStore, InMemoryTransactionRunner (with structuredClone), a fixed clock, mock the inventory command (not the real API), create WorkflowRuntime, use runtime.createWorkflow() then runtime.triggerEvent(). Should assert on instance.currentState and instance.context. Should NOT hit any real external API.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "How do I test timeout behavior in my duraflows workflow? I have a 'waiting_for_approval' state with a 2-hour timeout that should transition to 'escalated'. I want to simulate the timeout firing in my test without actually waiting 2 hours.",
      "expected_output": "Should show the advanceable clock pattern (start with a fixed date, then advance past the 2-hour mark), call processExpiredWorkflows() to trigger the timeout, and assert the state changed to 'escalated'. Should use InMemoryInstanceStore.findExpired() which respects the clock. Should NOT suggest using vitest fake timers or setTimeout mocking.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "I need to test that when my 'charge_payment' command returns { ok: false, code: 'INSUFFICIENT_FUNDS' }, the workflow transitions from 'charging' to 'payment_failed' (the errorState). Also need to verify the error is recorded in history. Can you write this test?",
      "expected_output": "Should create a mock command that returns { ok: false, code: 'INSUFFICIENT_FUNDS', message: '...' }, register it in the command registry, trigger the event, assert currentState is 'payment_failed', and query history to verify the outcome is 'failure' with the right error details. Should use the failureCommand helper or equivalent pattern.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "We just upgraded to duraflows 1.0 and added a `bestEffort: true` notification command in our checkout flow. I need three vitest tests: (1) when the notification command returns ok:false, the workflow still transitions normally and outcome is 'success'; (2) when the notification command throws an exception, same — workflow still completes successfully and the throw is captured in commandResults; (3) verify our new audit observer fires once per state entry, including initial createInstance and onEnter hops, and that the observer's transitionUuid matches what the command saw on ctx.transitionUuid for the same state entry. Use vitest, the in-memory test doubles, and the duraflows runtime directly.",
      "expected_output": "Should produce three tests covering: (1) bestEffort returned ok:false — outcome stays 'success', commandResults[i].ok is false, next mandatory command still runs; (2) bestEffort thrown — runtime captures it as { ok: false, code: 'BEST_EFFORT_THROWN', error: { name, message, stack? } } (serializable shape, NOT the raw Error), outcome still 'success'; (3) recording-observer fixture (a WorkflowObserver that pushes events into an array) registered via WorkflowRuntimeOptions.observers, asserting the events array contains entries for the createInstance entry (fromState: null), each event-driven transition, AND each onEnter hop. The transitionUuid correlation test: a command that records ctx.transitionUuid into a captured variable, then assert observer's matching event.transitionUuid equals it. Should use the v1.0.0 transition-fields (fromState/toState/transitionUuid) on WorkflowExecutionContext. Should NOT suggest the deprecated v0.x top-level observer placement on NestJS forRootAsync.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "We added `definitionStore` support to our duraflows 5.0.0 test setup. Write two things: (1) a vitest test proving `initialize()` is idempotent — calling it twice against the same runtime only syncs the definition store once — plus a second test showing that a fresh WorkflowRuntime built against the SAME definitionStore, but with the definition's content changed under the same version, rejects with WorkflowDefinitionError from initialize(); (2) show how an existing WorkflowInstance test fixture in our suite needs to be updated for the new required definitionVersion field, including when null would be the correct value instead of a number.",
      "expected_output": "Should produce: (1) An idempotency test spying on the definitionStore's `ensure` method (e.g. `vi.spyOn(store, 'ensure')`), calling `runtime.initialize()` twice, and asserting `ensure` was called exactly once per registered definition — not twice. A separate test builds a second WorkflowRuntime pointed at the same InMemoryDefinitionStore but registers a definition with the same name+version and different content (e.g. a changed state), then asserts `initialize()` rejects with `WorkflowDefinitionError`. (2) A WorkflowInstance fixture object that includes `definitionVersion: <number>` (e.g. `1`) as a required field on the type, with a comment explaining `definitionVersion: null` is only correct for a fixture deliberately modeling a legacy row created before definition versioning existed (which the runtime stamps with a real version on its next transition). Should NOT show a fixture that omits `definitionVersion` entirely, and should NOT describe the field as optional.",
      "files": []
    }
  ]
}
